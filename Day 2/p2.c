#include <stdio.h>

int main(){
    // Create a myNum variable and assign the value 5 to it
    int myNum = 5;

    // Declare a myOtherNum variable without assignim it a value
    int myOtherNum;

    // Assign the value of myNum to myOtherNum
    myOtherNum = myNum;
    printf("The value of myOtherNum is %d", myOtherNum);
    return 0;
}